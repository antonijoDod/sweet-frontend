import React, { ReactElement } from "react";
import { ProfileLayout } from "@components";

const MyProfile = (): ReactElement => {
    return <ProfileLayout>It's empty</ProfileLayout>;
};

export default MyProfile;
